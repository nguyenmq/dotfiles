#-----------------------------------------------------------
# Set vi entry mode
#-----------------------------------------------------------
set-window-option -g mode-keys vi

#-----------------------------------------------------------
# Set scrollback
#-----------------------------------------------------------
set -g history-limit 10000

#-----------------------------------------------------------
# Change the bind key to Ctrl-a
#-----------------------------------------------------------
unbind C-b
set -g prefix C-a
bind C-a send-prefix

#-----------------------------------------------------------
# Set the default TERM
#-----------------------------------------------------------
set -g default-terminal screen

#-----------------------------------------------------------
# Update the TERM variable of terminal emulator when
# creating a new session or attaching a existing session
#-----------------------------------------------------------
set -g update-environment 'DISPLAY SSH_ASKPASS SSH_AGENT_PID SSH_CONNECTION WINDOWID XAUTHORITY TERM'

#-----------------------------------------------------------
# Determine if we should enable 256-colour support
#-----------------------------------------------------------
if "[[ ${TERM} =~ 256color || ${TERM} == fbterm ]]" 'set -g default-terminal screen-256color'

#-----------------------------------------------------------
# key to source tmux configuration
#-----------------------------------------------------------
bind r source-file  ~/.tmux.conf

#-----------------------------------------------------------
# Use '-' to split horizontally, '|' to split vertically
#-----------------------------------------------------------
unbind %
bind \ split-window -h
bind - split-window -v

#-----------------------------------------------------------
# Set left status bar
##----------------------------------------------------------
set -g status-fg '#dddddd'
set -g status-bg '#1C252C'
set -g status-left ''
set-window-option -g window-status-current-bg '#9FE069'
set-window-option -g window-status-current-fg '#1C252C'
#set -g status-fg '#000000'
#set -g status-bg '#dcdcdc'
#set -g status-left ''
#set-window-option -g window-status-current-bg '#afafaf'

bind-key -t vi-copy 'v' begin-selection

set -g mouse-select-pane on

set -g window-status-current-format "#I:#W"
set -g window-status-format "#I:#W"
set-window-option -g automatic-rename off

#-----------------------------------------------------------
# smart pane switching with awareness of vim splits
#-----------------------------------------------------------
bind -n C-h run "(tmux display-message -p '#{pane_current_command}' | grep -iq vim && tmux send-keys C-h) || tmux select-pane -L"
bind -n C-j run "(tmux display-message -p '#{pane_current_command}' | grep -iq vim && tmux send-keys C-j) || tmux select-pane -D"
bind -n C-k run "(tmux display-message -p '#{pane_current_command}' | grep -iq vim && tmux send-keys C-k) || tmux select-pane -U"
bind -n C-l run "(tmux display-message -p '#{pane_current_command}' | grep -iq vim && tmux send-keys C-l) || tmux select-pane -R"
bind -n C-\ run "(tmux display-message -p '#{pane_current_command}' | grep -iq vim && tmux send-keys 'C-\\') || tmux select-pane -l"

#-----------------------------------------------------------
# Enable tmux yank
#-----------------------------------------------------------
run-shell ~/.dotfiles/tmux-yank/yank.tmux
