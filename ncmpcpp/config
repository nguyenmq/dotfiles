user_interface = classic
fetch_lyrics_for_current_song_in_background = no
follow_now_playing_lyrics = yes
lyrics_fetchers = genius, metrolyrics, azlyrics, musixmatch
show_duplicate_tags = no
media_library_primary_tag = album_artist
media_library_albums_split_by_date = no
playlist_disable_highlight_delay = 0
startup_screen = media_library
autocenter_mode = yes
default_find_mode = wrapped
screen_switcher_mode = playlist, media_library
ask_before_clearing_playlists = no

# visualizer settings
visualizer_fifo_path = /tmp/mpd.fifo
visualizer_output_name = my_fifo
visualizer_in_stereo = yes
visualizer_sync_interval = 30
visualizer_type = spectrum
visualizer_look = ▃▃
audio_output {
    type    "fifo"
    name   "Visualizer feed"
    path   "/tmp/mpd.fifo"
    format "48000:16:2"
}


# defines the format of each column in the playlist view
song_columns_list_format = "(5)[]{nl} (25)[]{Al} (40)[]{tl} (25)[]{bl} (5)[]{lr}"
# format the "songs" section in the library view
song_library_format = "{%n - }{%t}"
alternative_header_second_line_format = "%A - %b"
song_status_format = "%a - %t"

# colors
colors_enabled = yes
current_item_prefix = "$(green)$r$b"
current_item_suffix = "$/b$/r$(end)"
selected_item_prefix = $(white)$r$b
selected_item_suffix = $/b$/r$(end)
current_item_inactive_column_prefix = "$(green)"
current_item_inactive_column_suffix = "$(end)"
main_window_color = black
header_window_color = green
state_line_color = green
progressbar_color = white
progressbar_elapsed_color = green
progressbar_look = "▃▃▃"
alternative_ui_separator_color = green:b
window_border_color = green
active_window_border = green
state_flags_color = green
statusbar_color = green
now_playing_prefix = $b$(cyan)
now_playing_suffix = $/b$(end)
